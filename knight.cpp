#include<bits/stdc++.h>
using naespace std;
void display(int **grid)
bool isItSafe(int **grid,int row,int col,int n,int m)
{
    if(row<0 or col < 0 or row>=n or col>=m or grid[row][col]!==-1)
       return false;
    return true;
}

int main()
{
  int **grid=new *int[n];

}
